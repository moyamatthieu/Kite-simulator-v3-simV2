/**
 * ControlDirection.ts - Énumération des directions de contrôle
 *
 * Définit les directions simplifiées utilisées dans la logique
 * de contrôle de la barre de cerf-volant.
 */

export enum E_ControlDirection {
    LEFT = 'left',
    RIGHT = 'right',
    NEUTRAL = 'neutral',
}
